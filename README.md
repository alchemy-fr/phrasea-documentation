# Phrasea Documentation server

will serve static documentation generated/pushed by `documentation-builder`.

Every version (phrasea release tag) of documentation is available under `/docs/<version>/`.
